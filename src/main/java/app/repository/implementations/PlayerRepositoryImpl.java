package app.repository.implementations;

import app.model.Player;
import app.repository.generic.AbstractCrudRepository;
import app.repository.interfaces.PlayerRepository;

public class PlayerRepositoryImpl extends AbstractCrudRepository<Player> implements PlayerRepository {
}
